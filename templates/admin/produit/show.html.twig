{% extends 'base.html.twig' %}

{% block title %}Produit
{% endblock %}

{% block body %}
	<h1>{{ produit.nom }}</h1>

	<table class="table">
		<tbody>
			{# <tr>
							<th>Id</th>
							<td>{{ produit.id }}</td>
							</tr> #}
			{# <tr>
										<th>Nom</th>
										<td>{{ produit.nom }}</td>
										</tr> #}
				<tr>
					<th>
				Description
				</th>
					<td>
				{{ produit.description }}
			</td>
			</tr>
				<tr>
					<th>
				Image
				</th>
					<td>
				<img class='col-2' src="{{ asset('uploads/produit/' ~ produit.image) }}"> </td>

			</tr>

			<tr>
				<th>Stock</th>
				<td>{{ produit.stock }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_admin_produit_index') }}">back to list</a>

	<a href="{{ path('app_admin_produit_edit', {'id': produit.id}) }}">edit</a>

	{{ include('admin/produit/_delete_form.html.twig') }}
{% endblock %}
